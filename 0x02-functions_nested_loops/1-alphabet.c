#include  <stdio.h>
#include "main.h"

/**
 * main -Entry Point
 * description - code to print the alphabet in lowercase
 * using putchar
 * Return: 0
 */

int main(void)
{
	char ch = 'a';
	
	char i = '\n';

for (ch = 'a'; ch <= 'z'; ch++)

		putchar(ch);

		putchar(i);

	void print_alphabet(void);
	return (0);
}

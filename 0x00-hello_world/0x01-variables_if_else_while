#include <stdio.h>

/**
 * main - the entry point of the program
 * description -  code to print the alphabet in upper and lower case
 * Return: 0
 */

int main(void)
{
	char *str = "abcdefghijklmnopqrstuvwxyz"

			"ABCDEFGHIJKLMNOPQRSTUVWXYZ";
		puts(str);
	
		return (0);
}
